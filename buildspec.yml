version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo "🔧 Updating system packages..."
      - apt-get update -y

      - echo "🧹 Removing conflicting Node.js versions and binaries..."
      - apt-get remove -y nodejs npm || true
      - rm -f /usr/local/bin/node /usr/local/bin/npm /usr/local/bin/npx || true 

      - echo "📦 Installing Node.js 22..."
      - curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
      - apt-get install -y nodejs

      - echo "🔄 Ensuring correct PATH and refreshing shell lookup..."
      - hash -r

      - echo "🧾 Verifying Node.js and npm versions..."
      - which node
      - node -v
      - which npm
      - npm -v

      - echo "⚙️ Installing Clojure..."
      - curl -O https://download.clojure.org/install/linux-install-1.11.1.1413.sh
      - chmod +x linux-install-1.11.1.1413.sh
      - ./linux-install-1.11.1.1413.sh
      - clj -h || true

      - echo "📦 Installing Yarn..."
      - npm install --global yarn
      - yarn -v

      - echo "✅ Environment setup complete."

  pre_build:
    commands:
      - echo "🚀 Starting pre-build phase..."
      - export PATH=/usr/local/bin:$PATH 
      - node -v
      - cd $CODEBUILD_SRC_DIR
      
      # ⭐ NEW: Set execute permission for the build script
      - echo "⚙️ Setting execute permission for ./bin/build..."
      - chmod +x ./bin/build
      
      - echo "📁 Installing frontend dependencies..."
      - yarn install --frozen-lockfile || yarn install

  build:
    commands:
      - echo "🏗️ Building Metabase JAR..."
      - **bash ./bin/build** # Explicitly run with bash

  post_build:
    commands:
      - echo "📦 Moving build artifacts..."
      - mkdir -p artifacts
      - cp target/uberjar/metabase.jar artifacts/
      - echo "✅ Build complete."

artifacts:
  files:
    - artifacts/metabase.jar
    - appspec.yml
    - scripts/**/*
